# Loaded automatically by `pnpm --filter @dho/api test:integration` (see
# test/integration/env.setup.ts). These are fake, non-secret placeholder
# values matching CI's config (.github/workflows/ci.yml) — safe to commit.
# They are NOT used by the running app (apps/api/.env is, see .env.example).
DATABASE_URL=postgresql://dho:dho_dev_password@localhost:5433/dho_test
JWT_ACCESS_SECRET=test-only-access-secret-not-for-real-use
JWT_REFRESH_SECRET=test-only-refresh-secret-not-for-real-use
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=30d
APP_ORIGIN=http://localhost:3000
API_ORIGIN=http://localhost:4000
