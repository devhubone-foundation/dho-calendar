# Copy to apps/web/.env (Next.js and `docker compose --profile full` both
# read this file).
NODE_ENV=development
PORT=3000

NEXT_PUBLIC_API_ORIGIN=http://localhost:4000
NEXT_PUBLIC_WS_ORIGIN=ws://localhost:4000
